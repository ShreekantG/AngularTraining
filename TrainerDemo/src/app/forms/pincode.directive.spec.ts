import { PincodeDirective } from './pincode.directive';

describe('PincodeDirective', () => {
  it('should create an instance', () => {
    const directive = new PincodeDirective();
    expect(directive).toBeTruthy();
  });
});
